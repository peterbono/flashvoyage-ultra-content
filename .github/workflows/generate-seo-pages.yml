name: Generer les pages SEO

on:
  workflow_dispatch:
    inputs:
      template:
        description: 'Template specifique (vide = tous)'
        required: false
        default: ''
        type: choice
        options: ['', 'vols', 'budget', 'visa', 'esim']
      destination:
        description: 'Destination specifique (vide = toutes)'
        required: false
        default: ''
      limit:
        description: 'Nombre max de pages'
        required: false
        default: '5'
      force:
        description: 'Re-generer les pages existantes'
        required: false
        default: '0'
        type: choice
        options: ['0', '1']

  # CRON DESACTIVE — pages SEO generees manuellement par batch
  # schedule:
  #   - cron: '0 7 * * 3'  # Tous les mercredis a 7h UTC

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor-v2

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Generer les pages SEO
        env:
          WORDPRESS_URL: ${{ secrets.WORDPRESS_URL }}
          WORDPRESS_USERNAME: ${{ secrets.WORDPRESS_USERNAME }}
          WORDPRESS_APP_PASSWORD: ${{ secrets.WORDPRESS_APP_PASSWORD }}
          TRAVELPAYOUTS_API_TOKEN: ${{ secrets.TRAVELPAYOUTS_API_TOKEN }}
          JIRA_API_KEY: ${{ secrets.JIRA_API_KEY }}
          JIRA_DOMAIN: ${{ secrets.JIRA_DOMAIN }}
        run: |
          ARGS=""
          if [ -n "${{ github.event.inputs.template }}" ]; then
            ARGS="$ARGS --template ${{ github.event.inputs.template }}"
          fi
          if [ -n "${{ github.event.inputs.destination }}" ]; then
            ARGS="$ARGS --dest ${{ github.event.inputs.destination }}"
          fi
          ARGS="$ARGS --limit ${{ github.event.inputs.limit || '5' }}"
          if [ "${{ github.event.inputs.force }}" = "1" ]; then
            ARGS="$ARGS --force"
          fi
          node scripts/generate-seo-pages.js $ARGS

      - name: Commit state files
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/seo-matrix.json data/internal-links.json || true
          git diff --cached --quiet || git commit -m "chore: update seo-matrix after page generation"
          git push || true
